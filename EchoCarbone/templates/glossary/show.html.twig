{% extends 'base.html.twig' %}

{% block title %}Glossary{% endblock %}

{% block body %}
    <h1>Glossary</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ glossary.id }}</td>
            </tr>
            <tr>
                <th>Word</th>
                <td>{{ glossary.word }}</td>
            </tr>
            <tr>
                <th>Definition</th>
                <td>{{ glossary.definition }}</td>
            </tr>
            <tr>
                <th>Source</th>
                <td>{{ glossary.source }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ glossary.url }}</td>
            </tr>
            <tr>
                <th>Glossary_Category</th>
                <td>{{ glossary.glossaryCategory }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('glossary_index') }}">back to list</a>

    <a href="{{ path('glossary_edit', {'id': glossary.id}) }}">edit</a>

    {{ include('glossary/_delete_form.html.twig') }}
{% endblock %}
