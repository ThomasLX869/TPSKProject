<form method="post" action="{{ path('age_range_delete', {'id': age_range.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ age_range.id) }}">
    <button class="btn btn-danger col-12">Supprimer cette catégorie</button>
</form>
