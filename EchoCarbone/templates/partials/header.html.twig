<header>
	<nav class="navbar navbar-expand-xl navbar-dark bg-light position-fixed w-100">
		<div class="container-fluid">
			<a href="{{ path('home_index')}}"><img class="logo" src="../../assets/img/logo.png"></a>
			<button class="navbar-toggler burger" type="button" data-toggle="collapse" data-target="#navbarScroll" aria-controls="navbarScroll" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
			<div class="collapse navbar-collapse mx-4" id="navbarScroll">
				<ul class="navbar-nav ml-auto my-2 my-lg-0 navbar-nav-scroll">



					{% if app.user %}
						<li class="nav-item dropdown">
							<a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
								<i class="fa fa-user"></i>
								{{ app.user.pseudo | title }}
							</a>
							<div class="dropdown-menu dropdown-menu-right">
								<a class="dropdown-item" href="{{ path('account_profil' , {'slug': app.user.slug } ) }}">Mon Profil</a>
								{% if is_granted('ROLE_ADMIN') %}
								<a class="dropdown-item" href="{{ path('account_index')}}">Espace Admin</a>
								<a class="dropdown-item" href="{{ path("article_manager") }}">Gestion Articles</a>
								<a class="dropdown-item" href="{{ path('category_index') }}">Gestion Catégories</a>
								{% else %}
								<a class="dropdown-item" href="{{ path("article_manager") }}">Mes Articles</a>
								{% endif %}
								<div class="dropdown-divider"></div>
								<a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a>
							</div>
						</li>
					{% else %}
						<li class="nav-item dropdown invisible">
							<a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
								<i class="fa fa-user"></i>
								Connection
							</a>
						</li>

					{% endif %}

					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle" data-toggle="dropdown" href="{{ path('article_index_global') }}">Nos Articles</a>
						<div class="dropdown-menu dropdown-menu-right">
							<a class="dropdown-item" href="{{ path('article_index_global') }}">Tous les articles</a>
							<a class="dropdown-item" href="{{ path('article_index') }}">Articles écrits</a>
							<a class="dropdown-item" href="{{ path('video_index') }}">Articles vidéos</a>
							<a class="dropdown-item" href="{{ path("quizz_index") }}">Quizz</a>
							<a class="dropdown-item" href="{{ path('game_index') }}">Jeux</a>
						</div>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('home_youth')}}">Jeunesse</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('home_calc') }}">Calculateur</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('glossary_index') }}">Ecolexique</a>
					</li>
				</ul>
			</div>
		</div>
	</nav>
	</nav>
</header>


{% for label,messages in app.flashes %}
	<div class="container text-center my-5">
		<div class="alert alert-{{ label }}">
			{% for message in messages %}
				<p>{{ message | raw }}</p>
			{% endfor %}
		</div>
	</div>
{% endfor %}
